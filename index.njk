---
layout: base.njk
title: kspears.com
---

<div class="hero">
  <div class="hero-content">
    <img src="/static/images/kevin_head.png" alt="Kevin Spears" class="hero-photo">
    <div class="hero-text">
      <h1>Kevin Spears</h1>
      <p class="tagline">Cloud, technology, homelab, and life in the Pacific Northwest.</p>
    </div>
  </div>
</div>
<h2 class="section-title">Recent Posts</h2>
<ul class="post-list post-list--preview">
  {% for post in collections.posts | reverse %}
  {% if loop.index0 < 5 %}
  <li class="post-preview">
    <a href="{{ post.url }}" class="post-preview__title">{{ post.data.title }}</a>
    <div class="post-meta">{{ post.data.date | dateDisplay }}{% if post.data.category %} · {{ post.data.category }}{% endif %}</div>
    {% set excerptText = post.data.excerpt or (post.templateContent | default("") | excerptWords(50)) %}
    {% if excerptText %}
    <p class="post-preview__excerpt">{{ excerptText }}</p>
    {% endif %}
    <a href="{{ post.url }}" class="post-preview__read-more">Read more →</a>
  </li>
  {% endif %}
  {% endfor %}
</ul>
{% if collections.posts.length > 5 %}
<p class="more-posts"><a href="/posts.html">All posts ({{ collections.posts.length }})</a></p>
{% endif %}
